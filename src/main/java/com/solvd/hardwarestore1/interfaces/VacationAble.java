package com.solvd.hardwarestore1.interfaces;/*
An employee can hava vacations so acorrding the type of employee(speciality) and the
 days that has been working we can define the days to rest.
We do this using the int
working days it will return the amount of days to rest

 */

public interface VacationAble {
     int setVacation(String speciality);
}
