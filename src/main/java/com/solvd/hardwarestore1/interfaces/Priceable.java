package com.solvd.hardwarestore1.interfaces;

//An interface used to add the capacity to define the price of a product
public interface Priceable {
    double definePrice(double cost,double winPercentage);
}
